import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'FootballAdminProject_AlonsoSanchez',
  webDir: 'www'
};

export default config;
